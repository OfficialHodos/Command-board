# Command Board

Mobile-first personal command board PWA.

**Never Daunted.**

The app stores task data locally in the browser on the device. The repository contains the application code, not the user's live task data.
